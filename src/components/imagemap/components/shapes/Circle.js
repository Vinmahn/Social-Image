import React from 'react';
import Icon from '../../../icon/Icon';

const Circle = () => (
  <div className="Circle shape-container">
    <Icon className="icon" name="circle" />
  </div>
);

export default Circle;
