import React from 'react';
import Icon from '../../../icon/Icon';

const Triangle = () => (
  <div className="Triangle shape-container">
    <Icon className="icon" name="play" />
  </div>
);

export default Triangle;
